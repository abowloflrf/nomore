<nav class="top-navbar">
    <p>
        {{#if @blog.logo}}
            <a href="{{@blog.url}}"><img class="nav-logo" src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>
        {{else}}
            <a href="{{@blog.url}}">{{@blog.title}}</a>
        {{/if}}
    </p>
</nav>