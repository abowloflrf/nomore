<nav class="top-navbar">
    {{#if @blog.logo}}
        <a  class="nav-logo-a" href="{{@blog.url}}"><img class="nav-logo" src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>
    {{else}}
        <a class="nav-logo-a" href="{{@blog.url}}">{{@blog.title}}</a>
    {{/if}}
    
    {{navigation}}
</nav>